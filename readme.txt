=================================
obj - boat (본체), right(우측 노), left(좌측 노)
=================================
파일관련
matrix.h, matrix.cpp -> obj 띄우는 행렬들 
shader.h, shader.cpp -> shader 파일들 불러오는 코드
objloder.h, objloder.cpp -> obj 불러오는 파일들

main -> 주요파일임 이동처리 다 여기서함

==================================

<main 알아둬야할것>
obj 회전값들 0 0 0 인채로 그냥 가져오면 보트가 세로로 출력됨 -> obj 저장할때 문제생겼나봄
그래서 눕혀서 보정함.

idle 함수 작성 -> 이동처리, 객체 따라가는 카메라, 노 젓는 애니메이션 등 처리함.

c누르면 고정카메라 -> but. 원래는 객체 따라가는 카메라 위치 받아서 그위치에서 고정카메라 되게 하려했는데
아무리 시도해도 잘 안돼서 보완예정임

객체따라가는 카메라도 잘 가긴하는데 객체속도와 카메라 따라가는 속도가 다른지 객체가 카메라 쪽으로 점점 밀리는 상황발생함...
이것도 보완할 예정

직진시에, +y축으로 직진함
+y 직진, -y 후진, +x 왼쪽, -x 오른쪽, +z 윗쪽, -z 아랫쪽
헷갈리게해서 먄,,,, +x 축으로 바꾸려고 햇는데 잘 안되네 참고해주셈

===================================
텍스처맵핑 관련

open GL에서 텍스처 맵핑하려면 obj에 무슨 un? 인가 값도 있어야한대
직접 만든 obj파일에서는 그 값이 없어서 텍스처 맵핑이 안됨
그래서 양측 노의 색깔을 서로 다른 색으로 지정해 구분함.
===================================
모르겠거나 수정 필요하다싶은부분, 오류있으면 알려주센,,,,,,,,,,,,,,,